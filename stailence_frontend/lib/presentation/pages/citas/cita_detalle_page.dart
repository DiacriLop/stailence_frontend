/*import 'package:flutter/material.dart';

import '../../../core/constants/app_strings.dart';

class CitaDetallePage extends StatelessWidget {
  const CitaDetallePage({super.key});

  static const String routeName = '/citas/detalle';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text(AppStrings.citaDetalleTitle)),
      body: const Center(
        child: Text('Detalle de la cita'),
      ),
    );
  }
}
*/
